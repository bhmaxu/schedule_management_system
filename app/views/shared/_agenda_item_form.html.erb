<%= form_for(@agenda_item) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "创建新的日程..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>